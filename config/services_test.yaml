services:
    # Expose CartService publicly so functional tests can grab it directly
    # via static::getContainer()->get(CartService::class) without going
    # through the TestContainer's privateServices map.
    App\Service\CartService:
        public: true
